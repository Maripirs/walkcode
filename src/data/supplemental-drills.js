import { supplementalFullCode } from './supplemental-solutions.js';

// Additional standalone drills. Each prompt is original instructional copy;
// the source link in the UI points learners to the corresponding LeetCode task.
function exercise(prompt, code, choices, correct, why) {
  return {
    prompt,
    code,
    choices,
    correct,
    why,
    wrong: Object.fromEntries(choices.filter((choice) => choice !== correct).map((choice) => [
      choice,
      `${choice} is useful in other code, but it does not preserve the condition described in this drill.`,
    ])),
  };
}

function drill(title, topic, difficulty, context, exerciseData, pythonExercise, fullCode = supplementalFullCode[title] || null) {
  return { title, topic, difficulty, context, exercise: exerciseData, pythonExercise, ...(fullCode ? { fullCode } : {}) };
}

const e = exercise;
const d = drill;

export const supplementalCodeDrills = [
  d('Valid Palindrome', 'Two Pointers', 'Easy', 's is the original string. left and right move inward only across alphanumeric characters; comparisons are case-insensitive.',
    e('When the normalized characters differ, the string cannot be a palindrome.', 'if (s[left].toLowerCase() !== s[right].toLowerCase()) return ___;', ['false', 'true', 'null'], 'false', 'A single mismatched pair disproves the palindrome.'),
    e('When the normalized characters differ, the string cannot be a palindrome.', 'if s[left].lower() != s[right].lower():\n    return ___', ['False', 'True', 'None'], 'False', 'A single mismatched pair disproves the palindrome.')),
  d('Best Time to Buy and Sell Stock', 'Sliding Window', 'Easy', 'minPrice is the cheapest price seen before today. price is today’s sell price, and best is the largest valid profit so far.',
    e('Measure profit by selling today after buying at the best earlier price.', 'best = Math.max(best, price - ___);', ['minPrice', 'best', 'price'], 'minPrice', 'Subtracting the cheapest earlier buy price gives the profit for selling today.'),
    e('Measure profit by selling today after buying at the best earlier price.', 'best = max(best, price - ___)', ['min_price', 'best', 'price'], 'min_price', 'Subtracting the cheapest earlier buy price gives the profit for selling today.')),
  d('Merge Two Sorted Lists', 'Linked List', 'Easy', 'list1 and list2 point to the first unmerged nodes. tail is the last node already attached to the merged result.',
    e('When list1 has the smaller value, attach that existing node before advancing list1.', 'if (list1.val <= list2.val) {\n  tail.next = ___;\n  list1 = list1.next;\n}', ['list1', 'list2', 'tail'], 'list1', 'The smaller current node must become the next node in sorted order.'),
    e('When list1 has the smaller value, attach that existing node before advancing list1.', 'if list1.val <= list2.val:\n    tail.next = ___\n    list1 = list1.next', ['list1', 'list2', 'tail'], 'list1', 'The smaller current node must become the next node in sorted order.')),
  d('Maximum Depth of Binary Tree', 'Trees', 'Easy', 'root is the current node. The depth of a node is one plus the deeper of its left and right subtrees.',
    e('Combine the two subtree depths after the recursive calls return.', 'return 1 + Math.max(maxDepth(root.left), ___);', ['maxDepth(root.right)', 'maxDepth(root)', 'root.right'], 'maxDepth(root.right)', 'The right subtree must be measured recursively, just like the left subtree.'),
    e('Combine the two subtree depths after the recursive calls return.', 'return 1 + max(max_depth(root.left), ___)', ['max_depth(root.right)', 'max_depth(root)', 'root.right'], 'max_depth(root.right)', 'The right subtree must be measured recursively, just like the left subtree.')),
  d('Balanced Binary Tree', 'Trees', 'Easy', 'leftHeight and rightHeight are subtree heights. A difference greater than one means the current node is unbalanced.',
    e('Reject this subtree as soon as its two child heights differ by more than one.', 'if (Math.abs(leftHeight - rightHeight) > 1) return ___;', ['-1', '0', '1'], '-1', 'The sentinel -1 tells every ancestor that an imbalance was found.'),
    e('Reject this subtree as soon as its two child heights differ by more than one.', 'if abs(left_height - right_height) > 1:\n    return ___', ['-1', '0', '1'], '-1', 'The sentinel -1 tells every ancestor that an imbalance was found.')),
  d('Diameter of Binary Tree', 'Trees', 'Easy', 'left and right are the longest downward paths from this node. best stores the largest path through any node seen so far.',
    e('A path through this node uses one edge into each child subtree.', 'best = Math.max(best, ___);', ['left + right', 'Math.max(left, right)', 'left * right'], 'left + right', 'The diameter through a node joins both child paths, so their edge counts add.'),
    e('A path through this node uses one edge into each child subtree.', 'best = max(best, ___)', ['left + right', 'max(left, right)', 'left * right'], 'left + right', 'The diameter through a node joins both child paths, so their edge counts add.')),
  d('Climbing Stairs', '1-D Dynamic Programming', 'Easy', 'one and two are the number of ways to reach the two previous steps. The current step can be reached from either one.',
    e('Build the next count from the only two legal final moves: one step or two steps.', 'const current = ___;', ['one + two', 'one * two', 'Math.max(one, two)'], 'one + two', 'The two disjoint ways of arriving at this step are added together.'),
    e('Build the next count from the only two legal final moves: one step or two steps.', 'current = ___', ['one + two', 'one * two', 'max(one, two)'], 'one + two', 'The two disjoint ways of arriving at this step are added together.')),
  d('Min Cost Climbing Stairs', '1-D Dynamic Programming', 'Easy', 'one and two hold the cheapest total cost to reach the previous two positions. cost[i] is paid when stepping on i.',
    e('To stand on i, choose the cheaper prior landing and then pay cost[i].', 'const current = Math.min(one, two) + ___;', ['cost[i]', 'i', 'one + two'], 'cost[i]', 'The transition adds the cost of the stair being entered.'),
    e('To stand on i, choose the cheaper prior landing and then pay cost[i].', 'current = min(one, two) + ___', ['cost[i]', 'i', 'one + two'], 'cost[i]', 'The transition adds the cost of the stair being entered.')),
  d('House Robber', '1-D Dynamic Programming', 'Easy', 'take is the best total if the current house is robbed; skip is the best total if it is skipped. Adjacent houses cannot both be taken.',
    e('At each house, keep the better legal total instead of forcing a robbery.', 'const current = Math.max(take, ___);', ['skip', 'take + skip', 'nums[i]'], 'skip', 'Skipping this house is the competing legal choice, so compare it with taking it.'),
    e('At each house, keep the better legal total instead of forcing a robbery.', 'current = max(take, ___)', ['skip', 'take + skip', 'nums[i]'], 'skip', 'Skipping this house is the competing legal choice, so compare it with taking it.')),
  d('Missing Number', 'Bit Manipulation', 'Easy', 'missing starts at nums.length. Pairing every index with its value under XOR cancels every present number and leaves the absent one.',
    e('Fold both the array index and its value into the XOR accumulator.', 'missing ^= i ^ ___;', ['nums[i]', 'missing', 'i + 1'], 'nums[i]', 'Every present value must be paired with its index so matching values cancel.'),
    e('Fold both the array index and its value into the XOR accumulator.', 'missing ^= i ^ ___', ['nums[i]', 'missing', 'i + 1'], 'nums[i]', 'Every present value must be paired with its index so matching values cancel.')),
  d('3Sum', 'Two Pointers', 'Medium', 'nums is sorted first. i fixes one value, while left and right search the remaining suffix for the complementary pair.',
    e('After sorting, skip a repeated fixed value so it cannot produce the same triplet again.', 'if (i > 0 && nums[i] === nums[i - 1]) ___;', ['continue', 'break', 'return []'], 'continue', 'The repeated value should be ignored while later distinct fixed values are still valid.'),
    e('After sorting, skip a repeated fixed value so it cannot produce the same triplet again.', 'if i > 0 and nums[i] == nums[i - 1]:\n    ___', ['continue', 'break', 'return []'], 'continue', 'The repeated value should be ignored while later distinct fixed values are still valid.')),
  d('Container With Most Water', 'Two Pointers', 'Medium', 'left and right are the walls being considered. The shorter wall limits the area, so only moving it can possibly improve that limit.',
    e('When the left wall is shorter, discard it and try a taller possible left boundary.', 'if (height[left] <= height[right]) ___;', ['left++', 'right--', 'left--'], 'left++', 'Keeping the shorter left wall cannot increase the limiting height as width shrinks.'),
    e('When the left wall is shorter, discard it and try a taller possible left boundary.', 'if height[left] <= height[right]:\n    ___', ['left += 1', 'right -= 1', 'left -= 1'], 'left += 1', 'Keeping the shorter left wall cannot increase the limiting height as width shrinks.')),
  d('Product of Array Except Self', 'Arrays & Hashing', 'Medium', 'answer[i] already holds the product to the left of i. postfix is the product of values strictly to the right of i.',
    e('Finish answer[i] by multiplying its saved prefix by the suffix product.', 'answer[i] *= ___;', ['postfix', 'nums[i]', 'answer[i - 1]'], 'postfix', 'The suffix product supplies exactly the values that were not included in the prefix pass.'),
    e('Finish answer[i] by multiplying its saved prefix by the suffix product.', 'answer[i] *= ___', ['postfix', 'nums[i]', 'answer[i - 1]'], 'postfix', 'The suffix product supplies exactly the values that were not included in the prefix pass.')),
  d('Group Anagrams', 'Arrays & Hashing', 'Medium', 'word is one input string. key is a canonical signature; all anagrams must produce the same key before being grouped.',
    e('After sorting a word’s letters, turn that ordered list into one stable hash-map key.', "const key = [...word].sort().___('');", ['join', 'push', 'split'], 'join', 'join converts the sorted letters into the same comparable string for every anagram.'),
    e('After sorting a word’s letters, turn that ordered list into one stable hash-map key.', "key = ''.___(sorted(word))", ['join', 'append', 'split'], 'join', 'join converts the sorted letters into the same comparable string for every anagram.')),
  d('Top K Frequent Elements', 'Arrays & Hashing', 'Medium', 'counts maps each number to its frequency. buckets[count] holds every number with exactly that frequency.',
    e('Put this number into the bucket indexed by how often it appeared.', 'buckets[___].push(num);', ['count', 'num', 'k'], 'count', 'The bucket index is the frequency, not the number being stored.'),
    e('Put this number into the bucket indexed by how often it appeared.', 'buckets[___].append(num)', ['count', 'num', 'k'], 'count', 'The bucket index is the frequency, not the number being stored.')),
  d('Kth Largest Element in an Array', 'Heap / Priority Queue', 'Medium', 'target is the zero-based index of the kth largest value after partitioning. pivotIndex is where the chosen pivot finishes.',
    e('Stop Quickselect once the pivot lands at the requested sorted position.', 'if (pivotIndex === ___) return nums[pivotIndex];', ['target', 'k', 'left'], 'target', 'Quickselect is done when the pivot’s final index is the index being requested.'),
    e('Stop Quickselect once the pivot lands at the requested sorted position.', 'if pivot_index == ___:\n    return nums[pivot_index]', ['target', 'k', 'left'], 'target', 'Quickselect is done when the pivot’s final index is the index being requested.')),
  d('Search in Rotated Sorted Array', 'Binary Search', 'Medium', 'left, mid, and right are indexes in a rotated sorted array. At least one half around mid is still sorted.',
    e('Recognize the case where the left half is the normally sorted half.', 'if (nums[left] <= ___) {', ['nums[mid]', 'nums[right]', 'target'], 'nums[mid]', 'Comparing left with mid tells whether values rise normally across the left half.'),
    e('Recognize the case where the left half is the normally sorted half.', 'if nums[left] <= ___:', ['nums[mid]', 'nums[right]', 'target'], 'nums[mid]', 'Comparing left with mid tells whether values rise normally across the left half.')),
  d('Find Minimum in Rotated Sorted Array', 'Binary Search', 'Medium', 'right is a possible minimum. If nums[mid] is larger than nums[right], the rotation point must lie strictly to mid’s right.',
    e('When mid is already in the larger left run, discard it and everything before it.', 'if (nums[mid] > nums[right]) left = ___;', ['mid + 1', 'mid - 1', 'right'], 'mid + 1', 'mid cannot be the minimum in this case, so the search starts immediately after it.'),
    e('When mid is already in the larger left run, discard it and everything before it.', 'if nums[mid] > nums[right]:\n    left = ___', ['mid + 1', 'mid - 1', 'right'], 'mid + 1', 'mid cannot be the minimum in this case, so the search starts immediately after it.')),
  d('Min Stack', 'Stack', 'Medium', 'stack stores values. minStack stores the minimum value at the matching stack depth, so the current minimum is always on top.',
    e('For a new value, compare it with the minimum that existed before this push.', 'minStack.push(Math.min(val, ___));', ['minStack.at(-1)', 'stack.at(-1)', 'val + 1'], 'minStack.at(-1)', 'The prior minimum—not the prior ordinary value—must be carried to the new depth.'),
    e('For a new value, compare it with the minimum that existed before this push.', 'min_stack.append(min(val, ___))', ['min_stack[-1]', 'stack[-1]', 'val + 1'], 'min_stack[-1]', 'The prior minimum—not the prior ordinary value—must be carried to the new depth.')),
  d('Daily Temperatures', 'Stack', 'Medium', 'stack holds indexes whose next warmer day is unknown. i is today’s index, and prev is an earlier, cooler day just popped from the stack.',
    e('The waiting time is the distance from the earlier day to today.', 'result[prev] = ___;', ['i - prev', 'prev - i', 'i + prev'], 'i - prev', 'Today is the first warmer day, so subtract the earlier index from today’s index.'),
    e('The waiting time is the distance from the earlier day to today.', 'result[prev] = ___', ['i - prev', 'prev - i', 'i + prev'], 'i - prev', 'Today is the first warmer day, so subtract the earlier index from today’s index.')),
  d('Course Schedule', 'Graphs', 'Medium', 'indegree[course] counts prerequisites not yet completed. nextCourse is unlocked only after every incoming prerequisite edge is removed.',
    e('Completing one prerequisite removes exactly one incoming edge from its dependent course.', 'indegree[nextCourse] ___;', ['-= 1', '+= 1', '= 0'], '-= 1', 'Processing an edge satisfies one prerequisite, so the remaining prerequisite count decreases by one.'),
    e('Completing one prerequisite removes exactly one incoming edge from its dependent course.', 'indegree[next_course] ___', ['-= 1', '+= 1', '= 0'], '-= 1', 'Processing an edge satisfies one prerequisite, so the remaining prerequisite count decreases by one.')),
  d('Clone Graph', 'Graphs', 'Medium', 'clones maps each original node to its copied node. copy is the new node that will receive cloned neighbors.',
    e('Store the clone before exploring neighbors so a graph cycle can reuse it.', 'clones.set(node, ___);', ['copy', 'node', 'node.neighbors'], 'copy', 'Saving the new copy first prevents recursive traversal from cloning the same original node again.'),
    e('Store the clone before exploring neighbors so a graph cycle can reuse it.', 'clones[node] = ___', ['copy', 'node', 'node.neighbors'], 'copy', 'Saving the new copy first prevents recursive traversal from cloning the same original node again.')),
  d('Validate Binary Search Tree', 'Trees', 'Medium', 'low and high are exclusive bounds inherited from ancestors. node.val must fit between both, not just its direct parent.',
    e('Reject a node whose value violates the lower bound from an ancestor.', 'if (node.val <= ___ || node.val >= high) return false;', ['low', 'high', 'node.left'], 'low', 'The lower bound is the smallest value allowed anywhere in this subtree.'),
    e('Reject a node whose value violates the lower bound from an ancestor.', 'if node.val <= ___ or node.val >= high:\n    return False', ['low', 'high', 'node.left'], 'low', 'The lower bound is the smallest value allowed anywhere in this subtree.')),
  d('Coin Change', '1-D Dynamic Programming', 'Medium', 'dp[amount] is the fewest coins needed for that exact amount. coin is one denomination that can be placed last.',
    e('Use the already-solved smaller amount, then count the coin just added.', 'dp[amount] = Math.min(dp[amount], dp[amount - coin] + ___);', ['1', 'coin', 'dp[coin]'], '1', 'Adding one chosen coin increases the smaller solution’s coin count by exactly one.'),
    e('Use the already-solved smaller amount, then count the coin just added.', 'dp[amount] = min(dp[amount], dp[amount - coin] + ___)', ['1', 'coin', 'dp[coin]'], '1', 'Adding one chosen coin increases the smaller solution’s coin count by exactly one.')),
  d('Median of Two Sorted Arrays', 'Binary Search', 'Hard', 'aLeft and aRight border the chosen partition of the first array; bLeft and bRight border the other. A valid partition has every left value no larger than every right value.',
    e('At a valid partition, the lower median is the largest value on either left side.', 'const leftMax = ___;', ['Math.max(aLeft, bLeft)', 'Math.min(aRight, bRight)', 'aLeft + bLeft'], 'Math.max(aLeft, bLeft)', 'The median’s left half ends at the larger of its two boundary values.'),
    e('At a valid partition, the lower median is the largest value on either left side.', 'left_max = ___', ['max(a_left, b_left)', 'min(a_right, b_right)', 'a_left + b_left'], 'max(a_left, b_left)', 'The median’s left half ends at the larger of its two boundary values.')),
  d('Minimum Window Substring', 'Sliding Window', 'Hard', 'formed counts how many required character types currently meet their needed frequency. required is the number of distinct types in t.',
    e('Only shrink the window after it satisfies every required character count.', 'while (___) {\n  // try to improve the best window\n}', ['formed === required', 'formed < required', 'left < right'], 'formed === required', 'A window is eligible to shrink only while it is still fully valid.'),
    e('Only shrink the window after it satisfies every required character count.', 'while ___:\n    # try to improve the best window', ['formed == required', 'formed < required', 'left < right'], 'formed == required', 'A window is eligible to shrink only while it is still fully valid.')),
  d('Sliding Window Maximum', 'Sliding Window', 'Hard', 'deque holds candidate indexes in decreasing value order. Its front must always be the largest value still inside the current window.',
    e('Remove an index from the front once it has slid outside the left edge.', 'if (deque[0] < left) deque.___();', ['shift', 'pop', 'push'], 'shift', 'The expired index is at the front, so remove it from the front of the deque.'),
    e('Remove an index from the front once it has slid outside the left edge.', 'if deque[0] < left:\n    deque.___()', ['popleft', 'pop', 'append'], 'popleft', 'The expired index is at the front, so remove it from the front of the deque.')),
  d('Serialize and Deserialize Binary Tree', 'Trees', 'Hard', 'node is the current tree node. The serializer must record an explicit marker for a missing child so the tree shape can be rebuilt.',
    e('Emit a null marker instead of recursing through an absent child.', "if (!node) return ___;", ["'#'", '[]', 'node.val'], "'#'", 'The marker preserves the location of an empty child in the preorder stream.'),
    e('Emit a null marker instead of recursing through an absent child.', 'if node is None:\n    return ___', ["'#'", '[]', 'node.val'], "'#'", 'The marker preserves the location of an empty child in the preorder stream.')),
  d('Binary Tree Maximum Path Sum', 'Trees', 'Hard', 'left and right are nonnegative gains returned by the child subtrees. best tracks the strongest path that may turn through the current node.',
    e('Update the global answer with the path that uses this node and both beneficial child gains.', 'best = Math.max(best, ___);', ['node.val + left + right', 'Math.max(left, right)', 'left + right'], 'node.val + left + right', 'A turning path includes the current node plus one gain from each side.'),
    e('Update the global answer with the path that uses this node and both beneficial child gains.', 'best = max(best, ___)', ['node.val + left + right', 'max(left, right)', 'left + right'], 'node.val + left + right', 'A turning path includes the current node plus one gain from each side.')),
  d('Word Search II', 'Backtracking', 'Hard', 'node is the current trie node after following the board path. node.word is set only when that path completes one requested word.',
    e('Record a found word before continuing to longer trie paths.', 'if (node.word) result.push(___);', ['node.word', 'node.children', 'board[r][c]'], 'node.word', 'The trie stores the completed word at its terminal node.'),
    e('Record a found word before continuing to longer trie paths.', 'if node.word:\n    result.append(___)', ['node.word', 'node.children', 'board[r][c]'], 'node.word', 'The trie stores the completed word at its terminal node.')),
  d('Merge K Sorted Lists', 'Heap / Priority Queue', 'Hard', 'heap always exposes the smallest current node among k lists. node is the one just removed and appended to the answer.',
    e('After using a node, offer its successor as the next candidate from that list.', 'if (node.next) heap.push(___);', ['node.next', 'node.val', 'tail'], 'node.next', 'Only the successor can become this list’s next smallest candidate.'),
    e('After using a node, offer its successor as the next candidate from that list.', 'if node.next:\n    heappush(heap, ___)', ['(node.next.val, index, node.next)', 'node.next', '(node.val, index, node)'], '(node.next.val, index, node.next)', 'The heap needs a comparable value and list index alongside the successor node.')),
  d('Reverse Nodes in K-Group', 'Linked List', 'Hard', 'groupPrev points before the k-node block. prev becomes the reversed head of that block after its nodes are rewired.',
    e('Reconnect the preceding portion to the new front of the reversed block.', 'groupPrev.next = ___;', ['prev', 'groupNext', 'current'], 'prev', 'prev is the block’s new first node after the reversal loop.'),
    e('Reconnect the preceding portion to the new front of the reversed block.', 'group_prev.next = ___', ['prev', 'group_next', 'current'], 'prev', 'prev is the block’s new first node after the reversal loop.')),
  d('N-Queens', 'Backtracking', 'Hard', 'cols, diag1, and diag2 record attacked columns and diagonals from queens already placed in earlier rows.',
    e('Skip a square when any previously placed queen attacks it.', 'if (cols.has(col) || diag1.has(row - col) || ___) continue;', ['diag2.has(row + col)', 'cols.has(row + col)', 'diag1.has(row + col)'], 'diag2.has(row + col)', 'The second diagonal is identified by the invariant row + col.'),
    e('Skip a square when any previously placed queen attacks it.', 'if col in cols or row - col in diag1 or ___:\n    continue', ['row + col in diag2', 'row + col in cols', 'row + col in diag1'], 'row + col in diag2', 'The second diagonal is identified by the invariant row + col.')),
  d('Regular Expression Matching', '2-D Dynamic Programming', 'Hard', 'dp[i][j] says whether the first i input characters match the first j pattern characters. A star can represent zero copies of its preceding token.',
    e('For zero copies of a starred token, jump back over the token and star together.', 'dp[i][j] = dp[i][___] || (matches && dp[i - 1][j]);', ['j - 2', 'j - 1', 'i - 1'], 'j - 2', 'A star consumes two pattern positions: itself and the token it modifies.'),
    e('For zero copies of a starred token, jump back over the token and star together.', 'dp[i][j] = dp[i][___] or (matches and dp[i - 1][j])', ['j - 2', 'j - 1', 'i - 1'], 'j - 2', 'A star consumes two pattern positions: itself and the token it modifies.')),
  d('Edit Distance', '2-D Dynamic Programming', 'Hard', 'dp[i][j] is the fewest edits to turn the first i letters of word1 into the first j letters of word2.',
    e('For different final letters, pay one edit plus the cheapest of insert, delete, or replace.', 'dp[i][j] = 1 + Math.min(dp[i - 1][j], dp[i][j - 1], ___);', ['dp[i - 1][j - 1]', 'dp[i][j]', 'dp[i - 2][j - 2]'], 'dp[i - 1][j - 1]', 'Replacing aligns both final letters, so both prefixes lose one character.'),
    e('For different final letters, pay one edit plus the cheapest of insert, delete, or replace.', 'dp[i][j] = 1 + min(dp[i - 1][j], dp[i][j - 1], ___)', ['dp[i - 1][j - 1]', 'dp[i][j]', 'dp[i - 2][j - 2]'], 'dp[i - 1][j - 1]', 'Replacing aligns both final letters, so both prefixes lose one character.')),
  d('Distinct Subsequences', '2-D Dynamic Programming', 'Hard', 'dp[j] counts ways to form the first j target letters. Scanning j backward prevents one source character from being reused twice.',
    e('When the current source letter matches target[j - 1], extend every way that formed the shorter target.', 'dp[j] += ___;', ['dp[j - 1]', 'dp[j + 1]', '1'], 'dp[j - 1]', 'Each way to form the preceding target prefix can append this matching source letter.'),
    e('When the current source letter matches target[j - 1], extend every way that formed the shorter target.', 'dp[j] += ___', ['dp[j - 1]', 'dp[j + 1]', '1'], 'dp[j - 1]', 'Each way to form the preceding target prefix can append this matching source letter.'),
    {
      JavaScript: 'const dp = Array(t.length + 1).fill(0);\ndp[0] = 1;\nfor (const sourceChar of s) {\n  for (let j = t.length; j >= 1; j--) {\n    if (sourceChar === t[j - 1]) dp[j] += dp[j - 1];\n  }\n}\nreturn dp[t.length];',
      Python: 'dp = [0] * (len(t) + 1)\ndp[0] = 1\nfor source_char in s:\n    for j in range(len(t), 0, -1):\n        if source_char == t[j - 1]:\n            dp[j] += dp[j - 1]\nreturn dp[len(t)]',
    }),
  d('Interleaving String', '2-D Dynamic Programming', 'Hard', 'i and j are how many letters were consumed from s1 and s2. k equals i + j, the next index in s3.',
    e('If s1 supplies the next matching letter, advance only s1’s pointer.', 'if (i < s1.length && s1[i] === s3[k] && dfs(___, j)) return true;', ['i + 1', 'i, j + 1', 'k + 1'], 'i + 1', 'Taking from s1 consumes one more character of s1 while s2 remains at j.'),
    e('If s1 supplies the next matching letter, advance only s1’s pointer.', 'if i < len(s1) and s1[i] == s3[k] and dfs(___, j):\n    return True', ['i + 1', 'i, j + 1', 'k + 1'], 'i + 1', 'Taking from s1 consumes one more character of s1 while s2 remains at j.')),
  d('Burst Balloons', '2-D Dynamic Programming', 'Hard', 'left and right are fixed boundary balloons of an interval. mid is the balloon chosen to burst last inside that interval.',
    e('The last burst earns the product of its two fixed boundaries and itself.', 'coins[left] * coins[___] * coins[right]', ['mid', 'left', 'right'], 'mid', 'mid is the one still present between the interval boundaries at the final burst.'),
    e('The last burst earns the product of its two fixed boundaries and itself.', 'coins[left] * coins[___] * coins[right]', ['mid', 'left', 'right'], 'mid', 'mid is the one still present between the interval boundaries at the final burst.')),
  d('Maximum Profit in Job Scheduling', '1-D Dynamic Programming', 'Hard', 'dp[i] is the best profit using jobs through i. prev is the latest non-overlapping job before job i.',
    e('Taking job i adds its profit to the best compatible schedule before it.', 'dp[i] = Math.max(dp[i - 1] || 0, jobs[i][2] + (dp[___] || 0));', ['prev', 'i', 'i - 1'], 'prev', 'Only prev ends early enough to be combined legally with the current job.'),
    e('Taking job i adds its profit to the best compatible schedule before it.', 'dp[i] = max(dp[i - 1] if i else 0, earned + (dp[___] if prev >= 0 else 0))', ['prev', 'i', 'i - 1'], 'prev', 'Only prev ends early enough to be combined legally with the current job.')),
  d('Longest Increasing Path in a Matrix', 'Graphs', 'Hard', 'dfs(r, c) returns the longest increasing path starting at one cell. nextR and nextC are a strictly larger neighboring cell.',
    e('Moving to a larger neighbor contributes one edge plus the best path from that neighbor.', 'best = Math.max(best, ___);', ['1 + dfs(nextR, nextC)', 'dfs(r, c)', 'matrix[nextR][nextC]'], '1 + dfs(nextR, nextC)', 'The path gains one step for the move, then continues optimally from the neighbor.'),
    e('Moving to a larger neighbor contributes one edge plus the best path from that neighbor.', 'best = max(best, ___)', ['1 + dfs(next_r, next_c)', 'dfs(r, c)', 'matrix[next_r][next_c]'], '1 + dfs(next_r, next_c)', 'The path gains one step for the move, then continues optimally from the neighbor.')),
  d('Alien Dictionary', 'Graphs', 'Hard', 'first and second are the first different letters in two adjacent words. graph records the precedence edge that must hold in the alien alphabet.',
    e('Record that first must appear before the letter from the later word.', 'graph.get(first).add(___);', ['second', 'first', 'words'], 'second', 'The first mismatch establishes a directed edge from first to second.'),
    e('Record that first must appear before the letter from the later word.', 'graph[first].add(___)', ['second', 'first', 'words'], 'second', 'The first mismatch establishes a directed edge from first to second.')),
  d('Reconstruct Itinerary', 'Graphs', 'Hard', 'airport is the current airport. route is built after exhausting outgoing edges so it is produced in reverse Eulerian order.',
    e('Append an airport only after recursively consuming every ticket leaving it.', 'route.push(___);', ['airport', 'tickets', 'nextAirport'], 'airport', 'Postorder appending places an airport after all flights that must occur after it.'),
    e('Append an airport only after recursively consuming every ticket leaving it.', 'route.append(___)', ['airport', 'tickets', 'next_airport'], 'airport', 'Postorder appending places an airport after all flights that must occur after it.')),
];
