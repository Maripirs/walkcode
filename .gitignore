# Local tooling and preview state
node_modules/
.vite/
.wrangler/

# Workspace-only artifacts
outputs/
work/

# System files
.DS_Store
*.pem

# Local secrets / environment — never commit real keys (.env.example is the safe template)
.env
.env.local
.env.*.local
